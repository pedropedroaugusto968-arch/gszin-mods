(function() {
    alert("GSZIN MODS CARREGADO!");
    var key = prompt("DIGITE A KEY:");
    if (key === "trick4") {
        alert("XIT DISFARÇADO ATIVADO!");
    } else {
        location.reload();
    }
})();
