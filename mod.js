// FORÇAR LOGIN AO ABRIR
(function() {
    var senha = prompt("GSZIN MODS - DIGITE A KEY:");
    if (senha === "trick4") {
        alert("XIT DISFARÇADO ATIVADO!");
        // O restante do seu código de mira aqui...
    } else {
        alert("KEY INCORRETA!");
        location.reload();
    }
})();
